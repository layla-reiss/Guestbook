@{
    var x = ViewBag.guest;
}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>
<button type="button" id="previewButton" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
    Launch demo modal
</button>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="close" id="closeButton" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" id="otherCloseButton" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>
<script>
    $("#previewButton").on("click", function () {
        $("#exampleModal").modal("show");
    });

    $("#closeButton").on("click", function () {
        $("#exampleModal").modal("toggle");
    });

    $("#otherCloseButton").on("click", function () {
        $("#exampleModal").modal("toggle");
    });


</script>


@*FUCK THIS USE MODAL WHEN YOU NEXT SEE THIS CONVERT ALL THIS TO A MODAL LMFAO
@*<table>
    <tr>
        <td>
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h4>@x.name</h4>
                    <p>@x.email</p>
                    <p>@x.date</p>
                    <hr />
                    @if (x.comment.Length > 100)
                    {
                        <p>@x.comment.Substring(0, 100)</p>
                        <a href="DONT CLICK YET LMFAO">Show More...</a>
                    }
                </div>
            </div>
        </td>
        <td>
            <div class="card">
                <div class="card-body">
                    <h3>@x.name</h3>
                    <h5>@x.email</h5>
                    <h5>@x.date</h5>
                    <hr />
                    <p>@x.comment</p>
                </div>
            </div>
        </td>
    </tr>
</table>
<input type="submit" value="Confirm" asp-action="ValidateGuest")>*@